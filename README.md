# Immutable RevealLine Archive03

This successor retains the exact existing v0.33.0, v0.34.0 and v0.35.0 routes and adds independently playable v0.60.2 at `releases/v0.60.2/site/game/`. The main RevealLine release catalog supplies the new edition link. The original archive root and old release list remain byte-identical; they are historical navigation, not a new current-version selector.

All 803 previous public rows are preserved. The complete artifact contains 1,490 files and 783,088,453 bytes, below the unchanged 800,000,000-byte cap. Pinned original ZIPs are extracted sequentially through the reviewed extractor from source 2b8521b0a118df8663f93de5500e14da11741407; old editions are never rebuilt or retagged. Historical tools, original source-lock, expected inventory and README remain available. The current workflow uses `tools/originals/` instead of the earlier complete-tag-set rebuild.

`source-lock.json` binds four original tag/source/ZIP/manifest cohorts. Only v0.60.2 has a source qualification; none is fabricated for older editions. Existing `archive-routing.json` and `releases/index.json` are preserved through a two-path static allowlist with exact bytes/hashes. These files cannot replace arbitrary release content. The independent verifier requires the whole derived inventory, including both legacy JSON routes and hidden ownership files. The current source lock also pins the original evidence-derived inventory and 800MB limit.

The 24-case tiny local cohort passes without release downloads or game builds. It includes actual two-cohort copy protocol and preserved JSON byte/corruption/path/link/omission cases. The old four artifact-directory guard tests and old builder remain historical files; the active workflow runs the v2 extractor tests and complete `tools/originals/test_*.py` cohort. Actual four-original-ZIP extraction, public HTTP byte audit, browser play and final admission are separate deployment gates. No public acceptance is asserted by this source proposal.

The main-only workflow requires 3GiB free workspace, runs original extraction and exact verification, retains small per-ZIP/inventory receipts, uploads the canonical artifact including hidden files, and deploys under this repository only. A normal main merge triggers one run; do not add a duplicate manual dispatch. Existing main-game publication/default selector remains untouched.
