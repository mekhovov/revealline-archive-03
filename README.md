# Immutable RevealLine Archive03

The archive retains exact v0.33.0, v0.34.0, v0.35.0 and v0.60.2 editions. The shared `releases/` explorer now forwards to the main RevealLine release catalog, with a visible keyboard-accessible fallback link. It reuses the accepted Archive11 bridge byte for byte. The previous historical list is preserved in `legacy/releases-index.before-v0602-bridge.html`.

A native check of deployment 6502627452 found that the v0.60.2 title's Release explorer opened the old “Current v0.35.0” list with no main-catalog link. That deployment's full HTTP audit passed its original inventory, but this navigation finding prevents admission. This correction changes only the shared explorer's public bytes. All 1,489 other deployed rows remain exact, including all 798 older canonical release rows, both legacy JSON routes, the root index and `.nojekyll`. Original edition files are never rebuilt or retagged.

The corrected full artifact contains 1,490 files and 783,087,639 bytes, below the unchanged 800,000,000-byte cap. `source-lock.json` binds four original tag/source/ZIP/manifest cohorts and the new expected inventory. Only v0.60.2 has a source qualification; none is fabricated for older editions. `archive-routing.json` and `releases/index.json` retain their two-path static allowlist and exact hashes.

The current workflow still uses `tools/originals/`, the pinned original extractor from source 2b8521b0a118df8663f93de5500e14da11741407, sequential ZIP extraction, independent full-inventory verification, small receipts and the existing main-only Pages deployment. Its permissions, 3 GiB free-space requirement and 800 MB cap are unchanged. A normal main merge triggers one run; do not duplicate it with manual dispatch.

The active tiny fixture suite covers original-copy protocol, static-file corruption/path/link/omission rejection, exact preserved metadata and the accessible explorer destination. It downloads no release payloads and builds no game. Successor hosted extraction, fresh full HTTP byte verification and the native title → explorer → main catalog → browser Back journey remain separate required gates. No successor public acceptance is asserted here.
